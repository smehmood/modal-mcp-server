version: '3'

services:
  modal-mcp-server:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Mount the .modal directory to persist tokens/configuration
      - ~/.modal:/root/.modal
      # Uncomment this for hot reloading during development
      # - .:/app
    # Keep the container running
    restart: unless-stopped 